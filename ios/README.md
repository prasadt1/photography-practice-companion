# Iris — iOS (SwiftUI)

**Display name (when Xcode project ships):** `Iris` in `Info.plist`. **Bundle ID (unchanged):** `com.prasadtilloo.practicecompanion`.

Phase 4+ native iPhone app per `docs/spec.md` §3.4.

Phase 0 created this directory placeholder. Xcode project scaffolding lands in Phase 4.

**Prerequisites (manual):** Mac with Xcode 15+, Apple Developer Program enrollment.

**Fallback:** Capacitor PWA wrap only if no Mac is available.
